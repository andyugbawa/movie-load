<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
</head>
<body>


    <h1>EDIT PAGE</h1>
    <form action="/movies/<%= movie._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="movie[title]" id="title" placeholder="Title" value="<%= movie.title %>">
        </div>
        <div>
            <label for="genre">Genre</label>
            <input type="text" name="movie[genre]" id="genre" placeholder="Genre" value="<%= movie.genre %>">
        </div>
        <div>
            <label for="year">Year</label>
            <input type="number" name="movie[year]" id="year" placeholder="Year" value="<%= movie.year %>">
        </div>
        <button type=>Edit Movie</button>
        <a href="/movies/<%= movie._id %>">Back to movies</a>
    </form>
    


</body>
</html>